import React from "react";
const ActiveOrder = () => (
  <div>
    <h1 className="text-2xl font-bold mb-4">Active Order</h1>
    <p>No active orders at the moment.</p>
  </div>
);
export default ActiveOrder;
