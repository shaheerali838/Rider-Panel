import React from "react";

const Navbar = () => (
  <header className="bg-white shadow p-4 flex justify-between">
    <h2 className="text-xl font-bold">Rider Panel</h2>
    <div>Logout</div>
  </header>
);

export default Navbar;
